# Migrations Kit Bundle
# https://github.com/nowo-tech/migrations-kit-bundle
# Use SchemaChecker and CreateTablesService in Doctrine Migrations. See README for examples.

nowo_migrations_kit:
    # Doctrine connection name for CreateTablesService when registered as a service (see docs/CONFIGURATION.md)
    connection: default
